 r←{
   ⍝ replace: 'abc/def/gh' ←→ '-/'r'abc-def-gh'
     (isStrung ⍺)⍲isString ⍵:⎕SIGNAL 11
     0∊≢¨⍺ ⍵:⍵ ⍝ no-op
     2≠≢⍺:'not an old/new pair'⎕SIGNAL 11
     (old new)←⍺
     0=≢old:⍵ ⍝ no-op
     old≡new:⍵ ⍝ no-op
     isString ⍺:new@(⍸⍵=old)⊢⍵
     old ⎕R new⍠'Regex' 0⊢⍵
 }
