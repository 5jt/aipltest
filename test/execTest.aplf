 execTest←{
     ns←(breaks:0)⎕VSET{n←⍵.⎕NL⍳16 ⋄ (n ⋄ ⍵ ⎕VGET n)}⍵ ⍝ deep copy
     expn←'larg '/⍨×|ns.⎕NC'larg' ⍝ dyadic?
     expn,←'##.##.fun.',⍺,' rarg' ⍝ fun and tests are siblings
    ⍝ ⎕←'[',⍺,'] ',⍵.description
     ns.(result dm en)←ns{0::⍬ ⎕DM ⎕EN ⋄ (⍺.⍎⍵ ⋄ '' ⋄ 0)}expn
     ns.pass←markTest ns
     ns
 }
