 show←{
     dep←≡⍵
     rnk←≢shp←⍴⍵
     tly←≢⍵
     _←'scalar' 'vector' 'matrix'⊃⍨1+rnk
     _,←' depth ',⍕dep
     _,←', shape ',{⍬≡⍵:'⍬' ⋄ ⍕⍵}shp
     _,←', tally ',⍕tly
     qdr←⎕DR ⍵
     typ←{
         ⍵∊80 160 320:'char'
         ⍵≡83:'numeric'
         ⍕⍵
     }qdr
     _,←', type ',typ
     ⎕←_
     qdr≢326:⍵
     rnk dep shp tly qdr≡0 0 ⍬ 1 326:↑n{⍺ ⍵}¨⍵ ⎕VGET n←⍵.⎕NL-2
     ⍵
 }
